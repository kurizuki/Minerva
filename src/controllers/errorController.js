import registerError from '../services/errorService.js';

export function registerError(endPoint, httMethod, statusCode, error, request, response, IPAddress) {
    registerError(endPoint, httMethod, statusCode, error, request, response, IPAddress);
}